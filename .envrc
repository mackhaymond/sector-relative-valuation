if [[ ! -f pyproject.toml ]]; then
	log_error 'No pyproject.toml found. Run `uv init` to create one first.'
	exit 2
fi

if [[ ! -d .venv ]]; then
	uv sync
fi

export VIRTUAL_ENV="$PWD/.venv"
PATH_add "$VIRTUAL_ENV/bin"
